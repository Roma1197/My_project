def say_hello(self):
    class Student(Person):

        def __init__(self, univercity, year_of_study, speciality):
            self.univercity = univercity
            self.year_of_study = year_of_study
            self.speciality = speciality

    class Patient(Person):

        def __init__(self, hospital, doctor, diagnosis):
            self.hospital = hospital
            self.doctor = doctor
            self.diagnosis = diagnosis

    class BusDriver(Person):

        def __init__(self, first_name, last_name, age, driver_license, category, expirience_years):
            self.hospital = hospital
            self.doctor = doctor
            self.diagnosis = diagnosis
